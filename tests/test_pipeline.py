"""
TODO: as each module gets built, add a test here. Suggested first tests:

- test_schema_introspector_returns_known_table()
- test_executor_rejects_non_select_statements()
- test_executor_runs_valid_select()
- test_full_pipeline_on_a_simple_known_question()  (e.g. "how many customers
  are there" — you know the expected answer, so you can assert on it)

This doubles as your benchmark harness for the "compare against baseline"
deliverable from your proposal — run the same question set through the full
pipeline vs. a plain generate-and-execute version and compare pass rates.
"""
